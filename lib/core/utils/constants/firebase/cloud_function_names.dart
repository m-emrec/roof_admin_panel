///
/// This enum contains the names of the cloud functions that are used in the app.
///
/// **use this enum to call the cloud functions in the app.** To prevent typos.
enum CloudFunctionNames {
  createUserWithPhoneNumber,
}
